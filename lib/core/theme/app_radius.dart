import 'package:flutter/material.dart' show Radius;

class AppRadius {
  AppRadius._();

  static const Radius none = Radius.zero;

  static const Radius xs = Radius.circular(4);

  static const Radius sm = Radius.circular(6);

  static const Radius md = Radius.circular(8);

  static const Radius lg = Radius.circular(10);

  static const Radius xl = Radius.circular(12);

  static const Radius xxl = Radius.circular(16);

  static const Radius xxxl = Radius.circular(24);

  static const Radius full = Radius.circular(9999);
}
